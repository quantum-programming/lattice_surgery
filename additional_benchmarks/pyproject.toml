[project]
name = "additional-benchmarks"
version = "0.1.0"
description = "Comparison experiments for Reviewer 1"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mqt-qecc",
    "numpy",
    "surface-code-routing",
    "topols",
    "tqec",
]

[tool.uv.sources]
mqt-qecc = { path = "external/mqt_qecc", editable = true }
surface-code-routing = { path = "external/Surface_Code_Compiler", editable = true }
topols = { path = "external/TopoLS", editable = true }
tqec = { path = "external/tqec", editable = true }
